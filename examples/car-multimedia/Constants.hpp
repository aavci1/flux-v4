#pragma once

#include "Common.hpp"

namespace car {

inline constexpr std::uint32_t kSignatureBlue = 0x0A84FF;
inline constexpr float kWindowWidth = 1280.f;
inline constexpr float kWindowHeight = 720.f;
inline constexpr float kDividerThickness = 1.f;

} // namespace car
